<% if shipping_options.any? %>
Current shipping options for <b><%= country.name %></b>.

<% shipping_options.each do |shipping_option, details| %>
<b><%= details["shipping_method"] %> - <%= Money.new(details["usd_price"], "USD").format(symbol: '$') %></b>
Operated by <i><%= details["carrier"] %></i>
Estimated date of arrival <i><%= details["estimated_arrival_date"].strftime("%B %d") %></i>

<% end %>
<% else %>
No shipping options currently available for <b><%= country.name %></b>.
<% end %>

<%= render partial: "telegram/order/order_menu", locals: { customer: customer } %>